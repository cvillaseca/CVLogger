source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'CVLogger_Example', :exclusive => true do
  pod 'CVLogger', :path => '../'
end

target 'CVLogger_Tests', :exclusive => true do
  pod 'CVLogger', :path => '../'

  pod 'Specta'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
